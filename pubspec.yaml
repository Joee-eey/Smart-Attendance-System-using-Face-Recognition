name: userinterface
description: A new Flutter project.
 
publish_to: 'none' # Remove this line if not needed
version: 1.0.0+1
 
environment:
  sdk: '>=3.0.0 <4.0.0'
 
dependencies:
  flutter:
    sdk: flutter
 
  # Add these dependencies below the flutter sdk
  flutter_lints: ^6.0.0
  http: ^1.2.0
  image_picker: ^1.0.7
 
dev_dependencies:
  flutter_test:
    sdk: flutter
 
flutter:
  uses-material-design: true
 
  assets:
  - assets/images/